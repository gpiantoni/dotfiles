[user]
    email = github@gpiantoni.com
    name = Gio on Arch

[alias]
    wdiff = diff --color-words -w
    tree = log --graph --pretty='%C(yellow)%t%Creset%x09%C(cyan)%ad%C(reset) %C(auto)%d%C(reset) %s' --date=format:'%a %e-%b-%y  %H:%M' --all

[core]
    excludesfile = ~/.gitignore
